---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import SectionHeader from '../components/ui/SectionHeader.astro';
import PropertyCard from '../components/PropertyCard.astro';
import Button from '../components/ui/Button.astro';
import Badge from '../components/ui/Badge.astro';
import { createClient } from '@supabase/supabase-js';
import { getImageUrl, getSrcSet } from '../lib/supabase';

const HERO_BASE = 'https://qwhasgdxhvdavnofmisf.supabase.co/storage/v1/object/public/property-images/hero';
const heroLaForestada = `${HERO_BASE}/la-forestada.webp`;
const heroMariaI      = `${HERO_BASE}/maria-i.webp`;
const heroPampaIII    = `${HERO_BASE}/pampa-iii.webp`;
const heroAurelia     = `${HERO_BASE}/aurelia.webp`;

const SUPABASE_URL  = 'https://qwhasgdxhvdavnofmisf.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF3aGFzZ2R4aHZkYXZub2ZtaXNmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzkyNTI3MTgsImV4cCI6MjA5NDgyODcxOH0.Mj_lqGEtMhipASfO3YuBfVoCJ-f6fybOqLRw8OywCnw';

const sb = createClient(SUPABASE_URL, SUPABASE_ANON);

const DEFAULT_CARDS = [
  { id:'1', tag:'Unidades Funcionales', name:'Unidades Funcionales', description:'Unidades totalmente equipadas para personal corporativo en Añelo.', cover_image:'/complejo-donostia1.webp', cover_position:'50% 50%', dest_url:'/corporativo/#secComplejos', card_type:'Unidades Funcionales' },
  { id:'2', tag:'Complejos',            name:'Complejos Residenciales', description:'Complejos residenciales cerrados con todos los servicios incluidos.', cover_image:'/complejo-maria1.webp', cover_position:'50% 50%', dest_url:'/corporativo/#secComplejos', card_type:'Complejos' },
  { id:'3', tag:'Edificios',            name:'Edificios de Departamentos', description:'Departamentos corporativos en el microcentro de Añelo.', cover_image:'/edificio-pampa3.webp', cover_position:'50% 50%', dest_url:'/corporativo/#secEdificios', card_type:'Edificios' },
  { id:'4', tag:'Lotes',                name:'Lotes e Inversión', description:'Terrenos urbanizados para desarrollo en Vaca Muerta.', cover_image:'/complejo-yellowstone.webp', cover_position:'50% 50%', dest_url:'/venta/?tipo=terreno', card_type:'Lotes' },
];

function statusClass(s: string) {
  if (s === 'available') return 'disponible';
  if (s === 'occupied')  return 'ocupado';
  return 'parcialmente-ocupado';
}

let homepageCards: any[] = DEFAULT_CARDS;
try {
  const { data, error } = await sb.from('homepage_cards').select('*').eq('active', true).order('sort_order');
  if (!error && data && data.length > 0) homepageCards = data;
} catch(_) {}
---
<Layout
  title="Inmobiliaria Añelo | Alquiler Corporativo Oil & Gas · Inversión en Vaca Muerta"
  description="Alquiler corporativo para empresas Oil & Gas en Añelo, Neuquén. Complejos, edificios y departamentos equipados. Retorno 10–12% anual en USD. Cotizá en menos de 24 hs. +25 años en la Patagonia."
  pathname="/"
  preloadHero={true}
  heroImage={heroLaForestada}
>

  <!-- HERO -->
  <div class="hero">
    <!-- Background animation (desktop only) -->
    <div id="hero-bg" aria-hidden="true">
      <div class="hb-scene" id="hb-s0">
        <div class="hb-kb"><img src={getImageUrl(heroLaForestada, { width: 1920, quality: 65 })} alt="" width="1920" height="1080" loading="eager" decoding="async" fetchpriority="high" style="object-position:center 40%"></div>
        <div class="hb-vig-v"></div><div class="hb-vig-h"></div>
        <div class="hb-wipe"></div>
        <div class="hb-label">
          <div class="hb-eyebrow">Loteo · Desarrollo Urbano</div>
          <div class="hb-name">La Forestada</div>
          <div class="hb-meta-row"><div class="hb-line"></div><div class="hb-meta">Añelo · Neuquén</div></div>
        </div>
        <div class="hb-corner"><span class="hb-corner-ia">IA</span><div class="hb-corner-div"></div><span class="hb-corner-text">Inmobiliaria<br>Añelo</span></div>
      </div>
      <div class="hb-scene" id="hb-s1">
        <div class="hb-kb"><img src={getImageUrl(heroMariaI, { width: 1920, quality: 65 })} alt="" width="1920" height="1080" loading="lazy" decoding="async" style="object-position:center 60%"></div>
        <div class="hb-vig-v"></div><div class="hb-vig-h"></div>
        <div class="hb-wipe"></div>
        <div class="hb-label">
          <div class="hb-eyebrow">Edificio · Renta</div>
          <div class="hb-name">María I</div>
          <div class="hb-meta-row"><div class="hb-line"></div><div class="hb-meta">Departamentos</div></div>
        </div>
        <div class="hb-corner"><span class="hb-corner-ia">IA</span><div class="hb-corner-div"></div><span class="hb-corner-text">Inmobiliaria<br>Añelo</span></div>
      </div>
      <div class="hb-scene" id="hb-s2">
        <div class="hb-kb"><img src={getImageUrl(heroPampaIII, { width: 1920, quality: 65 })} alt="" width="1920" height="1080" loading="lazy" decoding="async" style="object-position:center 35%"></div>
        <div class="hb-vig-v"></div><div class="hb-vig-h"></div>
        <div class="hb-wipe"></div>
        <div class="hb-label">
          <div class="hb-eyebrow">Edificio · Renta</div>
          <div class="hb-name">Pampa III</div>
          <div class="hb-meta-row"><div class="hb-line"></div><div class="hb-meta">Departamentos</div></div>
        </div>
        <div class="hb-corner"><span class="hb-corner-ia">IA</span><div class="hb-corner-div"></div><span class="hb-corner-text">Inmobiliaria<br>Añelo</span></div>
      </div>
      <div class="hb-scene" id="hb-s3">
        <div class="hb-kb"><img src={getImageUrl(heroAurelia, { width: 1920, quality: 65 })} alt="" width="1920" height="1080" loading="lazy" decoding="async" style="object-position:center 55%"></div>
        <div class="hb-vig-v"></div><div class="hb-vig-h"></div>
        <div class="hb-wipe"></div>
        <div class="hb-label">
          <div class="hb-eyebrow">Vivienda · Renta</div>
          <div class="hb-name">Aurelia</div>
          <div class="hb-meta-row"><div class="hb-line"></div><div class="hb-meta">Casas residenciales</div></div>
        </div>
        <div class="hb-corner"><span class="hb-corner-ia">IA</span><div class="hb-corner-div"></div><span class="hb-corner-text">Inmobiliaria<br>Añelo</span></div>
      </div>
    </div>
    <div class="hero-overlay"></div>
    <div class="hero-badge-pos"><div class="hero-badge">Vaca Muerta · Añelo · Neuquén</div></div>
    <div class="hero-content">
      <h1>Invertí donde el<br/><em>petróleo</em> mueve<br/>la economía</h1>
      <p>Unidades residenciales y corporativas en el corazón del yacimiento más importante de Sudamérica. Rentabilidad en dólares, alta demanda.</p>
      <div class="hero-actions">
        <a class="btn-primary" href="/corporativo/">Ver Propiedades</a>
        <a class="btn-outline" href="/invertir/">Ver Retornos</a>
        <a class="btn-outline hero-action-tertiary" href="#por-que-anelo">¿Por qué Añelo?</a>
      </div>
    </div>
    <div class="hero-stats">
      <div class="hero-stat"><strong>8–12%</strong><span>Rentabilidad anual en USD</span></div>
      <div class="hero-stat"><strong>+25 años</strong><span>Experiencia en la Patagonia</span></div>
      <div class="hero-stat"><strong>Alta</strong><span>Demanda corporativa</span></div>
      <div class="hero-stat"><strong>2° reserva</strong><span>Shale del mundo · Vaca Muerta</span></div>
    </div>
  </div>

  <!-- QUICK CTA STRIP -->
  <div class="quick-cta-strip">
    <a class="quick-cta-btn alquilar" href="/corporativo/">
      <i data-lucide="key-round" class="cta-icon"></i>
      <span>
        Alquilar
        <span class="cta-sub">Ver propiedades disponibles</span>
      </span>
    </a>
    <a class="quick-cta-btn comprar" href="/invertir/">
      <i data-lucide="trending-up" class="cta-icon"></i>
      <span>
        Comprar / Invertir
        <span class="cta-sub">Conoce los retornos y contactanos</span>
      </span>
    </a>
  </div>

  <!-- STATS MOBILE: solo visible en mobile, debajo del strip -->
  <div class="hero-stats-mobile">
    <div class="hero-stat"><strong>8–12%</strong><span>Rentabilidad anual en USD</span></div>
    <div class="hero-stat"><strong>+25 años</strong><span>Experiencia en la Patagonia</span></div>
    <div class="hero-stat"><strong>Alta</strong><span>Demanda corporativa</span></div>
    <div class="hero-stat"><strong>2° reserva</strong><span>Shale del mundo · Vaca Muerta</span></div>
  </div>

  <div class="ticker-bar" role="marquee" aria-label="Información de contacto y servicios">
    <div class="ticker-track">
      <span class="ticker-item"><span class="ticker-dot">◆</span>Cotizá tu alquiler en menos de 24 hs</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Soluciones para empresas y particulares</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Alquiler corporativo Oil &amp; Gas — Vaca Muerta</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Invertí en Añelo desde USD 35.000 — 10–12% anual</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Complejos, edificios y UFs totalmente equipados</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>+25 años de trayectoria en la Patagonia</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Contratanos hoy y empezá a generar rentabilidad</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Gestión integral — Vós invertís, nosotros administramos</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Cotizá tu alquiler en menos de 24 hs</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Soluciones para empresas y particulares</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Alquiler corporativo Oil &amp; Gas — Vaca Muerta</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Invertí en Añelo desde USD 35.000 — 10–12% anual</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Complejos, edificios y UFs totalmente equipados</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>+25 años de trayectoria en la Patagonia</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Contratanos hoy y empezá a generar rentabilidad</span>
      <span class="ticker-item"><span class="ticker-dot">◆</span>Gestión integral — Vós invertís, nosotros administramos</span>
    </div>
  </div>

  <!-- PAMPA III DESTACADO -->
  <section class="py-16 bg-brand-black text-white border-y border-brand-gold/20 relative overflow-hidden" id="pampa-iii">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <div class="flex items-center justify-between mb-8 pb-4 border-b border-stone-800">
        <span class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-brand-gold">
          <i data-lucide="star" class="w-4 h-4"></i> Oportunidad Destacada
        </span>
        <Badge variant="available">Disponibilidad Inmediata</Badge>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Image Column -->
        <div class="relative rounded-2xl overflow-hidden border border-stone-800 shadow-2xl group">
          <img
            src="/edificio-pampa3.webp"
            alt="Edificio Pampa III · Centro de Añelo"
            class="w-full h-[400px] object-cover transition-transform duration-700 group-hover:scale-105"
            width="800"
            height="600"
            loading="lazy"
            decoding="async"
          />
          <div class="absolute bottom-4 left-4 flex items-center gap-2 px-3 py-1.5 bg-black/80 backdrop-blur-md rounded-md text-xs text-stone-200 border border-stone-700 font-medium">
            <i data-lucide="map-pin" class="w-3.5 h-3.5 text-brand-gold"></i>
            Centro de Añelo
          </div>
        </div>

        <!-- Content Column -->
        <div class="space-y-6">
          <div class="space-y-2">
            <span class="text-xs uppercase tracking-[0.2em] font-semibold text-brand-gold">Edificio Corporativo · Downtown</span>
            <h2 class="font-display text-4xl sm:text-5xl font-semibold text-white leading-tight">
              Edificio <em class="italic font-normal text-brand-gold">Pampa III</em>
            </h2>
          </div>

          <p class="text-stone-300 text-base leading-relaxed font-light">
            En el corazón de Añelo — a pasos de todo, sin perder tiempo en traslados. Ideal para equipos Oil & Gas que necesitan comodidad, practicidad y servicios completos desde el primer día.
          </p>

          <!-- Chips Grid -->
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 pt-2">
            <div class="flex items-center gap-2.5 p-3 bg-stone-900/80 border border-stone-800 rounded-lg text-xs font-medium text-stone-200">
              <i data-lucide="users" class="w-4 h-4 text-brand-gold"></i>
              Hasta 78 personas
            </div>
            <div class="flex items-center gap-2.5 p-3 bg-stone-900/80 border border-stone-800 rounded-lg text-xs font-medium text-stone-200">
              <i data-lucide="bed-double" class="w-4 h-4 text-brand-gold"></i>
              1 dormitorio y monoambientes
            </div>
            <div class="flex items-center gap-2.5 p-3 bg-stone-900/80 border border-stone-800 rounded-lg text-xs font-medium text-stone-200">
              <i data-lucide="layers" class="w-4 h-4 text-brand-gold"></i>
              Por piso o edificio completo
            </div>
            <div class="flex items-center gap-2.5 p-3 bg-stone-900/80 border border-stone-800 rounded-lg text-xs font-medium text-stone-200">
              <i data-lucide="wifi" class="w-4 h-4 text-brand-gold"></i>
              Todos los servicios incluidos
            </div>
          </div>

          <!-- Actions -->
          <div class="flex flex-wrap items-center gap-3 pt-4">
            <Button variant="primary" href="/corporativo/#edificios">
              Ver Disponibilidad
            </Button>
            <Button variant="whatsapp" href="https://wa.me/5492972431619?text=Hola%2C%20me%20interesa%20el%20Edificio%20Pampa%20III.%20%C2%BFPueden%20darme%20m%C3%A1s%20informaci%C3%B3n%3F" target="_blank" rel="noopener">
              WhatsApp
            </Button>
            <Button variant="outline" href="https://my.matterport.com/show/?m=HdzbizKQMrt" target="_blank" rel="noopener">
              Tour Virtual
            </Button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- POR QUE ANELO -->
  <section id="por-que-anelo" class="py-28 sm:py-36 bg-brand-black text-white relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <SectionHeader
        subtitle="La Oportunidad"
        title="Vaca Muerta: la oportunidad del siglo"
        description="Vaca Muerta es la segunda reserva de shale gas y la cuarta de shale oil del planeta. Las inversiones proyectadas superan los USD 100.000 millones en la próxima década. Añelo es la puerta de entrada a todo ese movimiento."
        align="center"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="p-8 bg-stone-900/60 border border-stone-800 hover:border-brand-gold/40 rounded-2xl space-y-4 transition-all duration-300 hover:-translate-y-1 shadow-xl">
          <div class="w-14 h-14 rounded-xl bg-brand-gold/15 text-brand-gold flex items-center justify-center border border-brand-gold/30">
            <i data-lucide="globe-2" class="w-7 h-7"></i>
          </div>
          <h3 class="font-display text-2xl font-semibold text-white">2° reserva mundial</h3>
          <p class="text-sm text-stone-300 leading-relaxed font-light">Vaca Muerta es la segunda reserva de shale del mundo, solo comparable con Permian Basin en Texas.</p>
        </div>

        <div class="p-8 bg-stone-900/60 border border-stone-800 hover:border-brand-gold/40 rounded-2xl space-y-4 transition-all duration-300 hover:-translate-y-1 shadow-xl">
          <div class="w-14 h-14 rounded-xl bg-brand-gold/15 text-brand-gold flex items-center justify-center border border-brand-gold/30">
            <i data-lucide="trending-up" class="w-7 h-7"></i>
          </div>
          <h3 class="font-display text-2xl font-semibold text-white">Crecimiento sostenido</h3>
          <p class="text-sm text-stone-300 leading-relaxed font-light">YPF, Shell, TotalEnergies, Chevron y Pan American Energy operan en la cuenca. La actividad crece año a año.</p>
        </div>

        <div class="p-8 bg-stone-900/60 border border-stone-800 hover:border-brand-gold/40 rounded-2xl space-y-4 transition-all duration-300 hover:-translate-y-1 shadow-xl">
          <div class="w-14 h-14 rounded-xl bg-brand-gold/15 text-brand-gold flex items-center justify-center border border-brand-gold/30">
            <i data-lucide="bar-chart-2" class="w-7 h-7"></i>
          </div>
          <h3 class="font-display text-2xl font-semibold text-white">Rentabilidad elevada</h3>
          <p class="text-sm text-stone-300 leading-relaxed font-light">Retornos de 8 a 12% anual respaldados por contratos corporativos. Una de las rentabilidades inmobiliarias más altas del país.</p>
        </div>

        <div class="p-8 bg-stone-900/60 border border-stone-800 hover:border-brand-gold/40 rounded-xl space-y-4 transition-all duration-300 hover:-translate-y-1 shadow-xl">
          <div class="w-14 h-14 rounded-xl bg-brand-gold/15 text-brand-gold flex items-center justify-center border border-brand-gold/30">
            <i data-lucide="key-round" class="w-7 h-7"></i>
          </div>
          <h3 class="font-display text-2xl font-semibold text-white">Gestión sin esfuerzo</h3>
          <p class="text-sm text-stone-300 leading-relaxed font-light">Nosotros conseguimos el inquilino, firmamos el contrato y te transferimos la renta. Tu propiedad trabaja por vos.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- PROPIEDADES PREVIEW -->
  <section id="nuestros-complejos" class="py-28 sm:py-36 bg-stone-950 text-white relative border-y border-stone-800/80">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <SectionHeader
        subtitle="Portafolio"
        title="Nuestro portafolio destacado"
        description="Categorías de inversión para distintos perfiles de capital. Todos con servicios incluidos y gestión profesional."
        align="center"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        {homepageCards.map((card: any) => (
          <PropertyCard
            name={card.name}
            type={card.card_type ?? 'Categoría'}
            coverImage={card.cover_image ? getImageUrl(card.cover_image, { width: 640 }) : '/complejo-maria1.webp'}
            coverPosition={card.cover_position ?? '50% 50%'}
            dest={card.dest_url || (card.card_type === 'Edificios' ? '/corporativo/#secEdificios' : card.card_type === 'Lotes' ? '/venta/?tipo=terreno' : '/corporativo/#secComplejos')}
            hideStatus={true}
            noModal={true}
          />
        ))}
      </div>

      <div class="text-center mt-16">
        <Button variant="primary" size="lg" href="/corporativo/">
          Ver todas las propiedades
        </Button>
      </div>
    </div>
  </section>

  <!-- PASOS -->
  <section id="como-invierto" class="py-28 sm:py-36 bg-brand-black text-white relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <SectionHeader
        subtitle="Proceso"
        title="Invertir es simple"
        description="Te acompañamos en cada etapa. Sin burocracia innecesaria, con total transparencia."
        align="center"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="p-8 bg-stone-900/60 border border-stone-800 rounded-2xl relative space-y-5 shadow-xl">
          <span class="block text-5xl font-display font-bold text-brand-gold/60">01</span>
          <h4 class="text-xl font-semibold text-white">Consulta inicial</h4>
          <p class="text-sm text-stone-300 font-light leading-relaxed">Analizamos tu perfil de inversión y te presentamos las opciones que mejor se adaptan a tu capital disponible.</p>
        </div>
        <div class="p-8 bg-stone-900/60 border border-stone-800 rounded-2xl relative space-y-5 shadow-xl">
          <span class="block text-5xl font-display font-bold text-brand-gold/60">02</span>
          <h4 class="text-xl font-semibold text-white">Visita o tour virtual</h4>
          <p class="text-sm text-stone-300 font-light leading-relaxed">Conocés la propiedad presencialmente o mediante recorrida virtual HD. Sin presión, a tu ritmo.</p>
        </div>
        <div class="p-8 bg-stone-900/60 border border-stone-800 rounded-2xl relative space-y-5 shadow-xl">
          <span class="block text-5xl font-display font-bold text-brand-gold/60">03</span>
          <h4 class="text-xl font-semibold text-white">Escritura y contrato</h4>
          <p class="text-sm text-stone-300 font-light leading-relaxed">Gestionamos el proceso legal completo y te asesoramos sobre opciones de pago y financiación disponibles.</p>
        </div>
        <div class="p-8 bg-stone-900/60 border border-stone-800 rounded-2xl relative space-y-5 shadow-xl">
          <span class="block text-5xl font-display font-bold text-brand-gold/60">04</span>
          <h4 class="text-xl font-semibold text-white">Tu renta llega</h4>
          <p class="text-sm text-stone-300 font-light leading-relaxed">Conseguimos el inquilino corporativo, firmamos el contrato y empezás a cobrar desde el primer mes.</p>
        </div>
      </div>
    </div>
  </section>

<script is:inline>
(function () {
  if (window.matchMedia('(max-width:768px)').matches) return;
  if (window.matchMedia('(prefers-reduced-motion:reduce)').matches) return;

  var SLOT  = 5.0;
  var FADE  = 0.9;
  var TOTAL = SLOT * 4; // 20 s

  var WIPE_DUR        = 1.1;
  var LABEL_IN_START  = 0.9;
  var LABEL_IN_DUR    = 0.8;
  var FADE_OUT_START  = SLOT - FADE; // 4.1 s
  var LINE_MAX        = Math.min(window.innerWidth / 1920 * 84, 84);

  // Ken Burns + zoom per scene (matched to React design)
  var CFG = [
    { panX: -40/1920*100, panY:   8/1080*100, zFrom: 1.08, zTo: 1.20 },
    { panX:  20/1920*100, panY: -10/1080*100, zFrom: 1.06, zTo: 1.16 },
    { panX: -18/1920*100, panY:  18/1080*100, zFrom: 1.05, zTo: 1.15 },
    { panX:  28/1920*100, panY:  -8/1080*100, zFrom: 1.06, zTo: 1.18 },
  ];

  var scenes = [0,1,2,3].map(function(i) {
    var el = document.getElementById('hb-s' + i);
    if (!el) return null;
    return {
      el:      el,
      kb:      el.querySelector('.hb-kb'),
      wipe:    el.querySelector('.hb-wipe'),
      label:   el.querySelector('.hb-label'),
      eyebrow: el.querySelector('.hb-eyebrow'),
      line:    el.querySelector('.hb-line'),
    };
  }).filter(Boolean);

  if (scenes.length < 4) return;

  function easeOutCubic(t)  { return 1 - Math.pow(1 - t, 3); }
  function easeInCubic(t)   { return t * t * t; }
  function easeOutQuart(t)  { return 1 - Math.pow(1 - t, 4); }
  function clamp(v, lo, hi) { return v < lo ? lo : v > hi ? hi : v; }
  function lerp(a, b, t)    { return a + (b - a) * t; }

  // Cross-fade opacity: each scene active for SLOT s, fading in/out over FADE s,
  // seamlessly looping via modular arithmetic.
  function sceneOpacity(i, T) {
    var d = ((T - i * SLOT) % TOTAL + TOTAL) % TOTAL;
    if (d > SLOT + FADE) return 0;
    if (d < FADE)        return easeOutCubic(d / FADE);
    if (d >= SLOT)       return 1 - easeInCubic((d - SLOT) / FADE);
    return 1;
  }

  var time   = 0;
  var lastTs = null;

  function tick(ts) {
    if (lastTs === null) lastTs = ts;
    var dt = Math.min((ts - lastTs) / 1000, 0.1);
    lastTs = ts;
    time = (time + dt) % TOTAL;

    var T = time;

    for (var i = 0; i < 4; i++) {
      var sc  = scenes[i];
      var cfg = CFG[i];
      var op  = sceneOpacity(i, T);

      sc.el.style.opacity = op;
      if (op < 0.01) continue;

      var d = ((T - i * SLOT) % TOTAL + TOTAL) % TOTAL;

      // Ken Burns
      var kbProg = clamp(d / (SLOT + FADE), 0, 1);
      var s  = lerp(cfg.zFrom, cfg.zTo, kbProg);
      var tx = cfg.panX * kbProg;
      var ty = cfg.panY * kbProg;
      sc.kb.style.transform = 'scale(' + s.toFixed(4) + ') translate(' + tx.toFixed(3) + '%, ' + ty.toFixed(3) + '%)';

      // Wipe reveal (cream cover slides off to the right)
      var wipeT = clamp(d / WIPE_DUR, 0, 1);
      sc.wipe.style.clipPath = 'inset(0 0 0 ' + (easeOutQuart(wipeT) * 100).toFixed(2) + '%)';

      // Label fade + translate
      var labelIn = clamp((d - LABEL_IN_START) / LABEL_IN_DUR, 0, 1);
      var exitT   = clamp((d - FADE_OUT_START) / FADE, 0, 1);
      var labelOp = easeOutCubic(labelIn) * (1 - easeInCubic(exitT));
      var labelY  = lerp(22, 0, easeOutCubic(labelIn)) - exitT * 14;
      sc.label.style.opacity   = labelOp;
      sc.label.style.transform = 'translateY(' + labelY.toFixed(2) + 'px)';

      // Eyebrow
      var eyeIn = clamp((d - 0.7) / 0.7, 0, 1);
      sc.eyebrow.style.opacity = easeOutCubic(eyeIn) * (1 - easeInCubic(exitT));

      // Accent line
      var lineIn = clamp((d - 1.2) / 0.9, 0, 1);
      sc.line.style.width = (easeOutQuart(lineIn) * LINE_MAX).toFixed(1) + 'px';
    }

    requestAnimationFrame(tick);
  }

  // Start once all scene images have loaded
  var imgs    = document.querySelectorAll('#hero-bg img');
  var pending = imgs.length;
  if (pending === 0) { requestAnimationFrame(tick); return; }

  function onDone() { if (--pending <= 0) requestAnimationFrame(tick); }
  imgs.forEach(function(img) {
    if (img.complete) { onDone(); }
    else { img.addEventListener('load', onDone); img.addEventListener('error', onDone); }
  });
})();
</script>
</Layout>
